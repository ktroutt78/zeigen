# V2.3 — Tauri-Rust IPC integration — Plan

**Drafted:** 2026-05-27
**Status:** Ready to execute
**Source of truth for decisions:** `docs/V2.3-CONTEXT.md`

V2.3 wires the V2.2 engine's widened error-code surface the rest of the way into the running app, salvages the one mid-record failure the engine deliberately leaves recoverable, and clears the three process-lifecycle backlog items the 187GB incident produced. The IPC pipe already exists end-to-end (`engine.rs` is a pure pass-through; `App.tsx` already renders every code), so this phase is frontend legibility + behavior plus Rust-side lifecycle hardening — no engine-binary changes, no new IPC events or fields. Three commits, each atomic and independently verifiable:

- **c1 — IPC error-code integration (end-to-end through Tauri).** Friendly per-code messages in the existing error banner (D-01), `MIC_SESSION_FAILED` partial-recording salvage gated on clean compositing (D-02), and the documented decision to keep `MIC_DROPPED` stderr-only (D-03, no code). Frontend-only plus one idempotent Rust seam fix.
- **c2 — process-lifecycle hardening.** The weight is D-04: orphan webcam-ffmpeg parent-death protection (the load-bearing fix for the 187GB class; primary acceptance is an actual SIGKILL test). D-05 is a small gap-closure — the sweeper already exists, so just per-stale-dir logging + harness self-cleanup. D-06: acceptance-run provenance (committed path + reconstructed `ac5e115` record).
- **c3 — R6 packaged-app permission UAT + report.** Local unsigned `com.zeigen.app` bundle, confirm-gated `tccutil` prep, manual GUI run, relaunch-gotcha re-check (D-07); `docs/V2-RISKS.md` updates (R3 downgrade, R6 close/update); `docs/V2.3-REPORT.md`.

**Atomicity guarantee.** c1 ships error-handling integration independent of lifecycle work. c2 ships lifecycle hardening independent of the error path. c3 is the verification/report capstone — it depends on c1 (the R3 downgrade requires the codes actually wired) and benefits from c2 being done. c1 and c2 could run in either order or in parallel; the recorded build order is **c1 → c2 → c3** (c3-last is forced by the R3-downgrade dependency on c1).

**Out-of-scope reminder** (V2-CHARTER, V2.3-CONTEXT): no engine-binary changes (`src-tauri/recording-engine/` is V2.2-done); no new IPC events or fields (D-03 declines the only candidate); no enumerated-UI-surface changes (picker/bubble/countdown/identify/review/export — the error banner and error-routing logic are not enumerated surfaces; the review window is reused unchanged); no save/edit-pipeline changes (`edit.rs`/`save_recording` invariant — D-02 reuses the existing `recording_finalize` → `openReview` handoff); no signing/notarization/DMG (D-07 builds unsigned local only); no V2.4 parity-gate work.

## Pre-phase verification (forward-merge)

Done 2026-05-27, no-op confirmed. Local `main` (`2484bb9`) == the `v1.0` tag == the branch point; `git log --all 2484bb9.. --not capture-engine-v2` is empty (no off-branch daily-driver fixes); `origin/main` (`d580c2e`) is a 169-commit-behind ancestor (push-hygiene only). Nothing to merge in. No ceremonial merge run.

---

## c1 — IPC error-code integration (end-to-end through Tauri)

The Rust IPC layer stays a pure pass-through; the V2.3 surface is the frontend `engine-event` handler (`App.tsx:783-869`) plus one idempotent Rust seam fix so the salvage path can reuse `recording_finalize`.

### Files edited

- `src/App.tsx` — error-code → message map (D-01); the `error`-case branch routing `MIC_SESSION_FAILED` to salvage vs. all other codes to discard (D-02).
- `src-tauri/src/lib.rs` — make `recording_finalize` (`:341`) idempotently stop the webcam segment before compositing, so the salvage path (which never calls `engine_stop`) still gets a finalized last segment (D-02).

### D-01 — friendly messages in the existing banner

Add a code → message lookup near the `engine-event` handler. The `error` event types `code` as a plain `string` (`App.tsx:738`), so the three new codes already typecheck — no type-union change.

```ts
const ERROR_MESSAGES: Record<string, string> = {
  PERMISSION_DENIED:
    "Screen Recording or Microphone permission isn't granted. Grant it in System Settings > Privacy & Security, then try again.",
  DISPLAY_NOT_FOUND: "That display is no longer available. Re-select a display and try again.",
  WINDOW_NOT_FOUND: "That window is no longer available. Re-select a window and try again.",
  MIC_NOT_FOUND: "The selected microphone is no longer available. Pick another mic and try again.",
  OUTPUT_PATH_INVALID: "Couldn't write the recording to disk. Check the destination folder exists and is writable.",
  WRITER_FAILED: "The recording couldn't be saved (writer error). Try again.",
  MIC_NO_FIRST_SAMPLE: "The microphone produced no audio. Check the mic and try recording again.",
  CLOCK_MISMATCH: "Couldn't sync the audio and video clocks. Try recording again.",
  MIC_SESSION_FAILED: "The microphone stopped mid-recording. Your recording was saved up to that point.",
  INVALID_COMMAND: "The recorder received an invalid command. Try again.",
  INVALID_STATE: "The recorder wasn't ready for that action. Try again.",
  INTERNAL: "The recorder hit an unexpected error and had to stop.",
};
```

In the `error` case (`App.tsx:858`), replace the raw banner with `setError(ERROR_MESSAGES[ev.code] ?? \`${ev.code}: ${ev.message}\`)`. An unmapped code still falls back to the legible raw form. Add a `console.error("[engine] error", ev.code, ev.message)` so the **raw code + message stay in logs** regardless of the friendly string. The `MIC_SESSION_FAILED` copy reflects the salvage outcome (D-02); if D-02's gate fails and it falls back to discard, change that one string to "…couldn't be recovered."

### D-02 — `MIC_SESSION_FAILED` salvage, gated on clean compositing

**The seam.** On the normal stop, `engine_stop` (`lib.rs:240-255`) gracefully stops the webcam segment (`webcam.stop_segment()`, writes `q\n`, finalizes the segment mp4) *before* the engine's `stopped` event triggers `recording_finalize` (`App.tsx:827-857`). On the error path, the engine self-resets to idle, so the frontend must **not** send `engine_stop` (it would produce a follow-on `INVALID_STATE`, the exact trap `recording_cleanup_local` was built to avoid). But `recording_finalize` and `recording_cleanup_local` both `.take()` the recording handle, so they can't be chained.

**Rust fix (idempotent finalize).** Make `recording_finalize` stop the webcam segment itself, before reading `segments()`:

- Currently `recording_finalize` moves `rec.webcam` into `webcam_opt` and reads `webcam.segments()`. Change it to bind the webcam mutably and call `webcam.stop_segment()` first, then read segments. `stop_segment` is already idempotent — on the normal path the segment was already stopped by `engine_stop`, so `self.current.take()` returns `None` and it's a no-op (`webcam.rs:69-79`). On the salvage path it finalizes the still-running last segment. This makes `recording_finalize` self-sufficient regardless of how it's reached.

**Frontend routing.** In the `error` case, branch on the code:

- `ev.code === "MIC_SESSION_FAILED"` → run the salvage flow, mirroring the `stopped` case (`App.tsx:827-857`): `setState("idle")`, `incReview()`, then `invoke<FinalizedRecording>("recording_finalize").then(info => openReview(\`review-${info.stamp}\`, info.scratch_mp4_path, decReview)).catch(err => { setError(...); decReview(); })`. Do **not** call `engine_stop`; do **not** call `recording_cleanup_local`. The engine has already finalized the partial screen file (D-04 semantics: "finalizes whatever has been written so far, then emits `error`"), so the file exists by the time the frontend sees the event.
- All other codes → unchanged discard path: `recording_cleanup_local` + `setState("idle")`.

The salvage branch reuses `recording_finalize`, `openReview`, and the review window unchanged — no save/edit change, no review-window change. The only new logic is the error-path branch and the idempotent `stop_segment` in finalize.

**The hard gate (DONE-WHEN, exercised not asserted).** Trigger `MIC_SESSION_FAILED` through the full Tauri app with webcam active and confirm the partial composites cleanly and opens in review:

- Launch the app with `ZEIGEN_FORCE_MIC_SESSION_ERROR=1` in the environment (it propagates to the spawned engine — `engine.rs` `Command::new` inherits parent env). The engine fires `MIC_SESSION_FAILED` ~100ms after `started` (per V2.2-REPORT), so the partial is short (~100ms of screen + ~100ms of webcam segment).
- Start a recording **with a webcam selected** (exercises `composite::composite`, the real risk).
- Expected: banner shows the friendly `MIC_SESSION_FAILED` message; the partial composites without error; the review window opens with a playable file.
- **Pass → salvage ships.** **Fail (composite chokes on the short/odd partial) → fall back to discard for `MIC_SESSION_FAILED`** (route it to `recording_cleanup_local` like other codes, change the D-01 string to the "couldn't be recovered" wording), and record the fallback in the report. No half-salvage that opens a broken review.
- Also smoke the webcam-inactive case (no segments → `recording_finalize` uses the screen mp4 directly): confirm it opens too. The webcam-active composite is the load-bearing assertion.

### D-03 — `MIC_DROPPED` stays stderr-only (no code)

Decision, not implementation. The engine keeps logging `MIC_DROPPED reason=<interrupted|sample-cessation>` to stderr (already captured by `engine.rs:78-83`); no IPC event, no frontend change. Rationale in CONTEXT D-03: recording survives (non-fatal), a new event is structural IPC widening that triggers an ADR, and there is no non-blocking UI surface to host a mid-record warning without a UI phase. Recorded as a deferred idea (event shape + UI affordance) for the future UI phase. c1 does nothing here except confirm the engine's stderr line still appears in the captured `[engine]` log during a mic-drop.

### c1 done-when

- Every error `code` renders a readable banner string; an unmapped code falls back to `CODE: message`; the raw code+message appear in the console/log.
- The three V2.2 codes (`CLOCK_MISMATCH`, `MIC_NO_FIRST_SAMPLE`, `MIC_SESSION_FAILED`) are confirmed reaching the frontend through the **real Tauri spawn path** (not the harness's direct-engine spawn) — `CLOCK_MISMATCH` via `ZEIGEN_FORCE_CLOCK_MISMATCH=1` on `start`, `MIC_SESSION_FAILED` via `ZEIGEN_FORCE_MIC_SESSION_ERROR=1`, `MIC_NO_FIRST_SAMPLE` observed or code-traced.
- `MIC_SESSION_FAILED` with webcam active either salvages cleanly (partial composites + review opens) or falls back to discard for that code — the outcome is recorded, not assumed.
- `MIC_DROPPED` remains stderr-only; the `[engine]` log shows the line on a mic-drop; no IPC event emitted.
- No engine-binary change, no new IPC event/field, no enumerated-UI-surface change.

### Commit message hint

`feat(v2.3 c1): IPC error-code integration — friendly messages + MIC_SESSION_FAILED salvage`

Body: the friendly-message map; the salvage gate outcome (shipped vs fell-back-to-discard) with the composite result; confirmation the three codes reach the frontend via the Tauri spawn path.

---

## c2 — process-lifecycle hardening

c2's weight is **D-04** — the only load-bearing fix here; D-05 is a small gap-closure (sweeper already exists) and D-06 is provenance plumbing. c2's primary acceptance is the **SIGKILL orphan test**.

### Files edited

- `src-tauri/src/webcam.rs` — spawn a parent-death watchdog alongside the ffmpeg child in `start_segment`; reap it in `stop_segment` and `Drop` (D-04).
- `src-tauri/src/thumbs.rs` — `sweep_dir_older_than` (`:121-139`) logs a line per removed entry (D-05a).
- `harness/v2-drift/run.py` — acceptance output target (D-06); self-clean its own scratch on teardown (D-05b).
- `harness/v2-drift/uat1.py`, `uat3.py`, `uat4.py` — self-clean their own scratch on teardown (D-05b).
- `.gitignore` — ignore `harness/v2-drift/acceptance/*.mp4`, keep acceptance CSVs tracked (D-06).

### Files created

- `harness/v2-drift/acceptance/c2-acceptance-ac5e115.csv` — the reconstructed c2 acceptance record (D-06).
- `harness/orphan-kill-test.sh` — scripted SIGKILL helper for the D-04 orphan test (PID-grab + kill -9 + assert ffmpeg gone, with a `--graceful` contrast mode).

### D-04 — orphan webcam-ffmpeg parent-death protection (the load-bearing fix)

The webcam ffmpeg is a child of the **Tauri/Rust process** (`webcam.rs:37-62`), not the Swift engine. The existing `Drop` (`webcam.rs:90-97`) and `stop_segment` (`:69-79`) cover graceful teardown, but neither runs when the Tauri process dies ungracefully (crash / force-quit / SIGKILL) — the 187GB scenario. macOS has no `PR_SET_PDEATHSIG`, and a Rust-side supervisor thread dies with the process under SIGKILL, so the death-signal **must live in a separate process**.

**Mechanism — detached sibling watchdog (parent-PID poll, CONTEXT D-04 primary).** Keep ffmpeg a direct Rust child (preserves the `q\n`-to-stdin graceful stop and the `Drop`-kill fast path). Additionally spawn a tiny `/bin/sh` watchdog that watches **both** the ffmpeg PID and the Tauri PID and kills ffmpeg if the Tauri PID dies first:

```rust
let tauri_pid = std::process::id();
let ff_pid = child.id();
let watchdog = Command::new("/bin/sh")
    .arg("-c")
    .arg(format!(
        "while kill -0 {ff} 2>/dev/null && kill -0 {tauri} 2>/dev/null; do sleep 1; done; \
         kill -9 {ff} 2>/dev/null",
        ff = ff_pid, tauri = tauri_pid,
    ))
    .stdin(Stdio::null()).stdout(Stdio::null()).stderr(Stdio::null())
    .spawn()
    .ok(); // best-effort; absence must not block recording
```

- **Crash/SIGKILL of Tauri:** only the Tauri PID receives the signal; its children (ffmpeg, watchdog) reparent to launchd and survive. The next poll sees `kill -0 {tauri}` fail, the loop exits, `kill -9 {ff}` stops the orphan. Disk reclaims once ffmpeg dies.
- **Graceful stop:** `stop_segment` writes `q\n`, waits ffmpeg; ffmpeg exits; the watchdog's `kill -0 {ff}` fails and it self-exits within ≤1s. To avoid blocking the stop path and to reap immediately, `stop_segment` (and `Drop`) also `kill()` + `wait()` the stored watchdog handle directly.
- Store the watchdog in the struct: `watchdog: Option<Child>`. Reap in both `stop_segment` and `Drop` so no zombie accumulates on the normal path; on Tauri crash, launchd reaps the reparented watchdog.

Poll interval 1s (consistent with the D-14/D-15 1.0s timeouts). PID-reuse caveat: a vanishingly small window where the Tauri PID could be reused before the watchdog notices — negligible on macOS (slow PID reuse); note it inline. kqueue `EVFILT_PROC` (CONTEXT D-04 fallback) only if the poll proves flaky.

**Both PIDs are captured at spawn time, not resolved later.** `format!` bakes the literal integer values of `std::process::id()` (Tauri) and `child.id()` (ffmpeg) into the `-c` string, so the shell watches fixed numbers decided at spawn. This is load-bearing: a watchdog that tried to discover the parent PID after launch (e.g. a `getppid`-style lookup) would, by the time the parent is dead, watch nothing — the value must be frozen while the parent is still alive.

**Test helper — `harness/orphan-kill-test.sh`.** The SIGKILL case can't be fully automated (starting a webcam recording needs the React UI), so the human starts the recording and the script does the deterministic kill+assert half. Shape:

```text
harness/orphan-kill-test.sh [--graceful]
```
1. Resolve the Tauri app PID (`pgrep -f` the app binary, or take as arg) and the webcam ffmpeg PID (`pgrep -f avfoundation`).
2. Assert the webcam ffmpeg is running; if absent, abort with "no webcam ffmpeg found — is a webcam recording active?" (the script cannot verify a *physical* camera is producing frames — the human confirms the webcam was actively capturing at kill time).
3. Record start time. Default: `kill -9 <tauri_pid>` (ungraceful — `Drop`/`stop_segment` do NOT run). `--graceful`: send a normal quit instead (contrast control — proves the test is catching the *ungraceful* path specifically, since graceful is already handled by `Drop`).
4. Poll `kill -0 <ff_pid>` every 0.2s up to 3s; on disappearance record elapsed → PASS; if still alive at 3s → FAIL (orphan survived).
5. Print a one-line result for the c2 commit body: e.g. `SIGKILL: ffmpeg <pid> gone <N>ms after Tauri kill -9 — PASS`.

### D-05 — sweeper gap-closure (the sweeper already exists)

No sweeper to build. `sweep_stale_scratch` (`lib.rs:425-434`) → `thumbs::sweep_dir_older_than(root, 24h)` (`thumbs.rs:121-139`) already runs at app launch (`lib.rs:565`). Two small gaps:

- **(a) Per-stale-dir logging.** Add an `eprintln!("[scratch-sweep] removing stale {} (age {}h)", path.display(), age_hours)` (matching the `[engine]` stderr convention) inside `sweep_dir_older_than` for each removed entry, so a cleaned zombie leaves a breadcrumb. Currently silent (`lib.rs:421-424`).
- **(b) Harness self-cleanup.** `run.py` / `uat*.py` create scratch via direct engine calls that bypass the Tauri app, so the launch sweeper never runs for them. Add a teardown step to each harness script that removes the scratch dirs it created (track the `output_path`s it passed to `start`, or sweep `~/Movies/Zeigen/.scratch/` for the run's own stamps on exit). The engine does **not** reach into the Movies dir (GA5 boundary).

Sharp distinction preserved (CONTEXT D-05): the sweeper is **residue cleanup only**. A dir's mtime doesn't update when a file inside it is written, so the sweeper can `remove_dir_all` a dir whose `webcam-00.mp4` an orphan is still writing — unlinking an open inode the process keeps writing to; disk isn't reclaimed until the process dies. **D-04 is what stops the orphan.**

### D-06 — acceptance-run provenance

- Create tracked `harness/v2-drift/acceptance/`. Add `c2-acceptance-ac5e115.csv` with the 10-row c2 acceptance table from `docs/V2.2-REPORT.md` §"Acceptance run", header annotated: reconstructed from commit `ac5e115`; raw run lost (overwritten by the endSession-removal control); authoritative for V2.4's R2 residual read. Columns mirror the harness schema (`take,display,abs_drift_ms`, plus `audio_start_s=0.000` per the report).
- `run.py`: add `--acceptance` (or a distinct `--out acceptance`) that writes `results.csv` / `meta.csv` into `acceptance/` instead of the gitignored `output/`. Ad-hoc/experimental runs keep landing in `output/`.
- `.gitignore`: add `harness/v2-drift/acceptance/*.mp4` (keep the big videos out) while leaving `acceptance/*.csv` tracked. `output/*.mp4` and `output/*.csv` stay ignored as today (`.gitignore:37-38`).
- **No re-run / no backfill** — the lost run can't be reproduced bit-for-bit; the commit-body table is the historical record; V2.4's fresh runs land in `acceptance/` natively.

### c2 done-when

- **Primary — SIGKILL orphan test (the 187GB path, not graceful quit), via `harness/orphan-kill-test.sh`:** the human starts a recording **with webcam actively capturing** (the script can't verify a physical camera); the script grabs the Tauri + webcam-ffmpeg PIDs, `kill -9 <tauri_pid>` (ungraceful — `Drop`/`stop_segment` do NOT run), then asserts ffmpeg is gone within ~1-2s and prints the elapsed time for the commit body. **Contrast control (`--graceful`):** a graceful quit also terminates ffmpeg, but via the pre-existing `Drop` path — running both proves the watchdog is what catches the *ungraceful* case specifically.
- Graceful stop still works: normal stop finalizes the segment (`q\n`), composite/review unaffected; no lingering watchdog (`pgrep -f 'kill -0'` clean after stop).
- D-05a: a stale `~/Movies/Zeigen/.scratch/recording-OLD/` dir (mtime forced >24h via `touch -t`) is logged (`[scratch-sweep] removing stale …`) and deleted on next app launch; a fresh (<24h) dir is left untouched.
- D-05b: after a `run.py` / UAT run, the scratch dirs it created are gone (no dev accumulation).
- D-06: `harness/v2-drift/acceptance/` is tracked and holds the reconstructed `ac5e115` record; `run.py --acceptance` writes there; `.gitignore` keeps acceptance mp4s out and CSVs in.

### Commit message hint

`feat(v2.3 c2): lifecycle hardening — webcam-ffmpeg death-signal watchdog + sweep logging + acceptance provenance`

Body: the SIGKILL test result (ffmpeg dies within Ns of Tauri SIGKILL); note D-05 was gap-closure not a build (sweeper pre-existed); the reconstructed acceptance record's provenance.

---

## c3 — R6 packaged-app permission UAT + report

The verification/report capstone. Depends on c1 (R3 downgrade needs the codes wired); benefits from c2.

### Files created

- `scripts/` or `harness/` confirm-gated TCC-reset + launch prep script for the R6 UAT (D-07).
- `docs/V2.3-REPORT.md` — outcomes per commit, the D-02 composite-gate result, the R6 finding, risk-register cross-references.

### Files edited

- `docs/V2-RISKS.md` — R3 downgrade (Medium → Low: the codes are now handled end-to-end in the frontend; Rust pass-through needed no change); R6 close-or-residual after the GUI run.

### D-07 — R6 clean-machine permission test against the packaged app

R6 is partially realized — the engine's pre-request order is code-verified, but the live clean-machine GUI run needs `com.zeigen.app` as the responsible process (against the CLI dev build TCC attributes to the host terminal/IDE).

- **Packaging — local unsigned bundle only.** `npm run tauri build` (or `cargo tauri build`) producing a `.app` with bundle id `com.zeigen.app`. No signing, no notarization, no DMG (deferred). Sufficient for correct TCC attribution.
- **Prep script (confirm-gated).** A small script that, **only after explicit confirmation**, runs `tccutil reset ScreenCapture com.zeigen.app` and `tccutil reset Microphone com.zeigen.app`, then launches the bundle. `tccutil reset` mutates the dev machine's permission state and is mildly destructive — the script must prompt (and I confirm with the user) before resetting. The daily driver (`v1.0` tag) uses different attribution, but confirm scope before resetting.
- **Manual GUI run (human-in-the-loop).** Launch the bundle clean; observe the permission dialog sequence — expect Screen Recording first, then Microphone (the engine's D-08 pre-request order: `CGRequestScreenCaptureAccess()` → `AVCaptureDevice.requestAccess(for: .audio)` → `.ready`); grant both; confirm a denial path emits `PERMISSION_DENIED` (now shown via the c1 friendly message) and the engine exits non-zero.
- **Relaunch-gotcha re-check.** V2.2 flagged that a Screen Recording grant may not take effect until the responsible process relaunches, suspected a dev-build artifact. Characterize it against the real `com.zeigen.app` bundle: does the grant take effect live, or require a relaunch? Record the finding verbatim in the R6 update.

### c3 done-when

- A local unsigned `com.zeigen.app` `.app` builds.
- The clean-machine permission sequence is characterized against the real bundle (order, copy, denial → `PERMISSION_DENIED` + non-zero exit), with any `tccutil reset` run only after explicit confirmation.
- The relaunch behavior is settled (live vs. relaunch-required) and recorded.
- `docs/V2-RISKS.md` R3 downgraded (codes wired end-to-end) and R6 closed-or-residual.
- `docs/V2.3-REPORT.md` written: per-commit outcomes, the D-02 gate result, the R6 finding, risk cross-references.

### Commit message hint

`docs(v2.3 c3): R6 packaged-app permission UAT + report + RISKS R3/R6`

Body: the permission-sequence finding; the relaunch-gotcha verdict against `com.zeigen.app`; R3/R6 status changes.

---

## Risk register touch points

- **R3 (IPC widening)** — downgraded in c3 from Medium to Low: the three V2.2 codes are now handled end-to-end (friendly messages + salvage), and the Rust pass-through needed no change, so the cascade is closed without further widening. D-03 adds no new event/field.
- **R4 (mic hot-swap)** — unchanged (downgraded at V2.2 c3). D-03's stderr-only decision does not reopen it; D-15(b) `AVCaptureDeviceWasDisconnected` observation remains a deferred optimization, not a V2.3 gap.
- **R6 (permission flow)** — closed-or-residual in c3 via the packaged-app GUI run; the relaunch behavior is the open question this phase settles.

## Final notes

- No engine-binary commit anywhere in V2.3 (the `recording-engine/` tree is V2.2-frozen). The only Rust changes are in `src-tauri/src/` (`webcam.rs`, `lib.rs`, `thumbs.rs`); the only frontend change is `src/App.tsx`.
- `docs/IPC-SPEC.md` is not expected to change (D-03 declines a new event; no new codes). If D-02 prompts a clarifying note on `MIC_SESSION_FAILED` consumer behavior, it lands there in c1.
- Cosmetic, out of scope but worth doing someday: `git push origin main` to retire the 169-commit remote staleness.

---

*Phase: V2.3-tauri-rust-ipc-integration*
*Plan drafted: 2026-05-27*
